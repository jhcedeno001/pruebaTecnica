apiVersion: v1
kind: Service
metadata:
  name: app-riesgo-proveedores
  labels:
    app: app-riesgo-proveedores
spec:
  type: NodePort
  selector:
    app: app-riesgo-proveedores
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30642
